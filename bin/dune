(executables
 (public_names bddl parsepp lexpp  compall compare rewrall bddlrewr)
 (names bddl parsepp lexpp  compilebench tests bench_rewr bddlrewr)
 (libraries lib unix str))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
